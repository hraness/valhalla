[package]
name = "vhalla-attention"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Bounded owner notifications and private exact read state."

[dependencies]
vhalla-social = { path = "../vhalla-social" }
vhalla-core = { path = "../vhalla-core" }
sha2 = { version = "0.10", default-features = false }

[dev-dependencies]
ed25519-dalek = { version = "2.2", default-features = false, features = ["alloc"] }
proptest = "1.6"

[lints]
workspace = true
