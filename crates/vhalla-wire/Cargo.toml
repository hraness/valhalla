[package]
name = "vhalla-wire"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Bounded, deterministic wire envelopes for vhalla (valhalla)."

[dependencies]
vhalla-core = { path = "../vhalla-core" }

[dev-dependencies]
hegeltest = "0.46.0"
proptest = "1.6"

[lints]
workspace = true
