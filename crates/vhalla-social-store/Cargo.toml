[package]
name = "vhalla-social-store"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Explicit private-file persistence for bounded signed social archives."

[dependencies]
vhalla-social = { path = "../vhalla-social" }
vhalla-core = { path = "../vhalla-core" }
sha2 = "0.10"

[dev-dependencies]
ed25519-dalek = { version = "2.2", default-features = false, features = ["alloc"] }
getrandom = "0.3"
proptest = "1.6"

[lints]
workspace = true
