[package]
name = "vhalla-discovery"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Bounded private discovery projections over verified social evidence."

[dependencies]
sha2 = { version = "0.10", default-features = false }
vhalla-core = { path = "../vhalla-core" }
vhalla-social = { path = "../vhalla-social" }

[dev-dependencies]
ed25519-dalek = { version = "2.2", default-features = false, features = ["alloc"] }
hegeltest = "0.46.0"
proptest = "1.6"

[lints]
workspace = true
