[package]
name = "vhalla-policy"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Local-only Valhalla authorization and typed effect capabilities."

[dependencies]
vhalla-core = { path = "../vhalla-core" }

[lints]
workspace = true
