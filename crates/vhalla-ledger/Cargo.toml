[package]
name = "vhalla-ledger"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Bounded canonical event history and derived checkpoint roots for Valhalla."

[dependencies]
sha2 = { version = "0.10", default-features = false }
vhalla-core = { path = "../vhalla-core" }

[dev-dependencies]
hegeltest = "0.46.0"
proptest = "1.6"

[lints]
workspace = true
