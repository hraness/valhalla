[package]
name = "vhalla-custody"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Shared Unix local-custody primitives for owner-only directories, private files, stable reads, and exclusive/shared locks."

[dependencies]
libc = "0.2"

[lints]
workspace = true
