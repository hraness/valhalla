[package]
name = "vhalla-witness"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Witness-mode program execution: the habitat-v1 finite-rule VM restated as the verifier-owned platform, with canonical codecs, digests, task manifests, a move-only run capability, and receipts"

[lints]
workspace = true

[dependencies]
sha2 = { version = "0.10", default-features = false }

[dev-dependencies]
proptest = "1.6"
