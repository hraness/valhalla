# Genome bundle prototype

Disposable model for canonical, content-addressed genome/proteome manifests, capability/import/resource admission, versioned upgrade, revocation, rollback, and organelle lifecycle receipts. It does not execute WASM, provide cryptographic signatures, or replace a sandbox.
