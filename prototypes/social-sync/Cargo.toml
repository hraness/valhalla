[package]
name = "valhalla-social-sync-prototype"
version = "0.0.0"
edition = "2021"
publish = false
license = "MIT"

[workspace]

[dependencies]
ed25519-dalek = { version = "2.2", default-features = false, features = ["alloc"] }
sha2 = { version = "0.10", default-features = false }

[dev-dependencies]
proptest = "1.6"
