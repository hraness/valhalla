[application]
name = "vhalla (valhalla) Preview"

[bundle]
identifier = "com.vhalla.prototype.native"
short_description = "Local signed-fixture preview for the shared Valhalla UI"

[macos]
# Match the actual ARM64 Mach-O minimum; this is not cross-version qualification.
minimum_system_version = "11.0"
