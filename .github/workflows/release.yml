name: Release

# Every tag reuses the complete Rust/site gate at that exact commit. Builders
# stage all artifacts; only the publisher can write a GitHub release.
on:
  push:
    tags: ["v*"]

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    uses: ./.github/workflows/rust.yml

  cli:
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
    runs-on: ${{ matrix.os }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Build vhalla
        # Preserve the maintained non-game commands. Legacy Platonik replay is
        # opt-in; the complete all-features verification gate still runs above.
        run: cargo build --release --locked -p vhalla-cli --no-default-features --features experimental-network,experimental-sync,experimental-rooms-tui,experimental-public --target "${{ matrix.target }}"
      - name: Smoke check
        shell: bash
        env:
          HRANESS_SUPPORT: "off"
        run: |
          set -euo pipefail
          cli="./target/${{ matrix.target }}/release/vhalla"
          help="$("$cli" --help)"
          printf '%s\n' "$help"
          [[ "$help" == *"vhalla experimental "* ]]
          [[ "$help" == *"vhalla social "* ]]
          [[ "$help" == *"vhalla rooms "* ]]
          [[ "$help" == *"vhalla public "* ]]
          [[ "$help" != *"vhalla game replay "* ]]
          if "$cli" game --help > game-disabled.txt 2>&1; then
            cat game-disabled.txt
            exit 1
          fi
          [[ "$(cat game-disabled.txt)" == *"require an explicit build with --features experimental-game"* ]]
      - name: Package
        shell: bash
        run: |
          set -euo pipefail
          out="valhalla-${GITHUB_REF_NAME}-${{ matrix.target }}"
          mkdir "$out"
          cp "target/${{ matrix.target }}/release/vhalla" "$out/"
          tar -czf "${out}.tar.gz" "$out"
          shasum -a 256 "${out}.tar.gz" > "${out}.tar.gz.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: release-cli-${{ matrix.target }}
          path: |
            valhalla-*.tar.gz
            valhalla-*.tar.gz.sha256
          if-no-files-found: error
          retention-days: 7

  menubar:
    needs: validate
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin
      - name: Build vhalla-menubar
        run: cargo build --release --locked --manifest-path desktop/Cargo.toml --target aarch64-apple-darwin
      - name: Smoke check
        # The GUI cannot run --help headless; verify its executable format.
        run: |
          set -euo pipefail
          bin=desktop/target/aarch64-apple-darwin/release/vhalla-menubar
          test -s "$bin"
          file "$bin" | grep -q "Mach-O 64-bit executable arm64"
      - name: Package
        shell: bash
        run: |
          set -euo pipefail
          out="valhalla-menubar-${GITHUB_REF_NAME}-aarch64-apple-darwin"
          mkdir "$out"
          cp desktop/target/aarch64-apple-darwin/release/vhalla-menubar "$out/"
          tar -czf "${out}.tar.gz" "$out"
          shasum -a 256 "${out}.tar.gz" > "${out}.tar.gz.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: release-menubar-aarch64-apple-darwin
          path: |
            valhalla-menubar-*.tar.gz
            valhalla-menubar-*.tar.gz.sha256
          if-no-files-found: error
          retention-days: 7

  publish:
    needs: [validate, cli, menubar]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      checks: read
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: release-assets
          merge-multiple: true
      - name: Verify gates, stage draft, and publish complete release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: python3 .github/scripts/publish_release.py release-assets
